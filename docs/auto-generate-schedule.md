# Автоматическая генерация расписания

## Функционал

Добавлена возможность автоматической генерации расписания с помощью кнопки "Автоматически сгенерировать".

## Как это работает

### 1. Кнопка автогенерации
- **Расположение**: В правом верхнем углу страницы расписания
- **Иконка**: ⚡ Sparkles
- **Текст**: "Автоматически сгенерировать"

### 2. Условия активации
Кнопка **отключена (disabled)**, если:
- В расписании уже есть хотя бы один урок, добавленный вручную
- В процессе генерации (показывает "Генерация...")

Кнопка **активна**, если:
- Расписание полностью пустое
- Не выполняется генерация

### 3. Tooltip при отключенной кнопке
При наведении на отключенную кнопку отображается подсказка:
> "Очистите расписание, чтобы использовать автогенерацию"

### 4. Кнопка "Очистить расписание"
- **Расположение**: В левом верхнем углу страницы
- **Иконка**: 🗑️ Trash
- **Видимость**: Показывается только если в расписании есть уроки
- **Подтверждение**: Открывает диалог с подтверждением действия

## Компоненты

### `ScheduleGenerateButton`
Кнопка для автоматической генерации расписания.

**Props:**
- `onGenerate: () => void` - обработчик генерации
- `isDisabled: boolean` - блокировка кнопки (есть уроки)
- `isGenerating: boolean` - состояние процесса генерации

### `ScheduleClearButton`
Кнопка для очистки расписания.

**Props:**
- `onClear: () => void` - обработчик очистки
- `hasLessons: boolean` - показывать ли кнопку

## API

### Service: `AutoGenerateScheduleService`
**Файл:** `src/api/services/schedule/auto-generate-schedule.service.ts`

**Метод:** `generateSchedule()`
- **Возвращает:** `Promise<ScheduleSlot[]>`
- **Описание:** Запрос к бэкенду для генерации расписания

### Hook: `useGenerateSchedule`
**Файл:** `src/api/queiries/schedule/useGenerateSchedule.ts`

**Использование:**
```typescript
const { mutate: generateSchedule, isPending: isGenerating } = useGenerateSchedule();

// Вызов генерации
generateSchedule(undefined, {
  onSuccess: (generatedSchedule) => {
    // Обработка успешной генерации
    setSchedule(generatedSchedule);
  }
});
```

**Особенности:**
- Автоматическая инвалидация кеша расписания
- Toast-уведомления об успехе/ошибке
- Обработка ошибок

## Логика работы

### Проверка наличия уроков
```typescript
const hasManualLessons = useMemo(() => {
  return schedule.some(slot => slot.lessons.length > 0);
}, [schedule]);
```

### Генерация
```typescript
const handleGenerate = () => {
  generateSchedule(undefined, {
    onSuccess: generatedSchedule => {
      setSchedule(generatedSchedule);
    },
  });
};
```

### Очистка
```typescript
const handleClear = () => {
  setSchedule(prev =>
    prev.map(slot => ({
      ...slot,
      lessons: [],
    }))
  );
};
```

## Бэкенд интеграция

### Endpoint (TODO)
```
POST /api/schedule/generate
```

**Ожидаемый ответ:**
```json
[
  {
    "dayOfWeek": "MONDAY",
    "lessonNumber": 1,
    "lessons": [
      {
        "id": "uuid",
        "subject": { "id": "uuid", "name": "Математика" },
        "teachers": [...],
        "rooms": [...],
        "participants": [...]
      }
    ]
  },
  ...
]
```

### Временная реализация (Mock)
В данный момент используется мок-данные с имитацией задержки сети (1.5 секунды).

**Файл:** `src/lib/mocks/index.ts`

**Экспорт:** `mockGeneratedSchedule`

**Особенности мок-расписания:**
- ✅ Полное расписание для 3 классов (5А, 5Б, 6А)
- ✅ Распределение по 5 дням недели
- ✅ Разумное количество уроков (3-5 в день)
- ✅ Деление на подгруппы (Английский язык в 5А)
- ✅ Учителя ведут несколько уроков параллельно (реалистично)
- ✅ Соблюдение недельной нагрузки по предметам
- ✅ Разнообразие предметов и учителей
- ✅ Использование разных кабинетов

**Проверка:**
1. Нажмите "Автоматически сгенерировать"
2. Подождите 1.5 секунды
3. Расписание заполнится данными
4. Можно сохранить или очистить для повторной генерации

## UX Flow

1. **Пользователь открывает страницу расписания**
   - Если расписание пустое → кнопка "Автоматически сгенерировать" активна
   - Если есть уроки → кнопка отключена, показывается "Очистить расписание"

2. **Пользователь нажимает "Автоматически сгенерировать"**
   - Кнопка показывает "Генерация..."
   - Отправляется запрос на бэкенд
   - При успехе:
     - Расписание заполняется
     - Показывается toast: "Расписание успешно сгенерировано!"
   - При ошибке:
     - Показывается toast с текстом ошибки

3. **Если нужно перегенерировать**
   - Нажать "Очистить расписание"
   - Подтвердить очистку в диалоге
   - Нажать "Автоматически сгенерировать"

## TODO

- [ ] Реализовать endpoint на бэкенде `/api/schedule/generate`
- [ ] Добавить параметры генерации (алгоритм, приоритеты)
- [ ] Добавить прогресс-бар при длительной генерации
- [ ] Возможность сохранить несколько вариантов расписания
- [ ] Оценка качества сгенерированного расписания
